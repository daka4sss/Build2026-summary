# Local editor / agent settings
.claude/settings.local.json

# OS files
Thumbs.db
.DS_Store

# Playwright MCP browser-automation artifacts
.playwright-mcp/

# Session slide decks (large binaries, can exceed GitHub's 100MB limit)
*.pptx
